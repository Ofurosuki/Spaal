# HDL-64E 復元品質分析レポート

## 分析結果サマリー

### 全体的な復元率
- **元の点数**: 125,635 ポイント
- **復元点数**: 91,799 ポイント
- **総合復元率**: 73.07%

### 距離別復元率

| 距離範囲 | 元ポイント数 | 復元ポイント数 | 復元率 |
|----------|-------------|---------------|--------|
| 0-20m    | 115,597     | 86,281        | **74.6%** ✅ |
| 20-40m   | 8,016       | 5,080         | 63.4% ⚠️ |
| 40-60m   | 1,529       | 430           | **28.1%** ❌ |
| 60-100m  | 493         | 8             | **1.6%** ❌ |

### 仰角別の問題エリア
- **最下部** (-29° ~ -25°): 復元率 < 70%
- **最上部** (+1° ~ +3°): 復元率 < 70%

---

## 原因分析

### 1. 遠距離での復元率低下の主要因

#### 原因A: 水平角度の離散化による点密度の低下

**問題**:
- 水平解像度: **0.2°** (1800ステップ)
- 遠距離では、隣接する水平ステップ間の物理的距離が大幅に増加

**計算例**:
```
距離10m: 0.2° = 3.5cm の間隔
距離50m: 0.2° = 17.5cm の間隔  ← 点が疎になる
距離100m: 0.2° = 35cm の間隔   ← さらに疎
```

**結果**:
- 遠距離では複数の実ポイントが同じグリッドセルに投影される
- **最近傍のみ保持**するため、他の点は失われる

#### 原因B: 垂直角度の不均一な分布

**分析結果**:
```
垂直角度間隔: min=0.234°, max=1.054°, mean=0.427°
```

- 最小間隔（0.234°）: 高密度エリア
- 最大間隔（1.054°）: 低密度エリア（約4.5倍疎）

**影響**:
- 垂直角度の間隔が大きいエリアでは、点の捕捉確率が低下
- 特に最上部・最下部で顕著

#### 原因C: KITTIデータの点群分布

**特徴**:
- 車載センサー → 近距離（0-20m）に点が集中
- 地面や建物など水平方向の物体が多い
- 空や遠方の点が少ない

**問題**:
- グリッド設計が近距離向けに最適化されている
- 遠距離データが少なく、統計的にも不利

---

## 改善策の提案

### 改善策1: 適応的水平解像度 ⭐⭐⭐

**概要**: 距離に応じて水平解像度を調整

**実装案**:
```python
def adaptive_horizontal_resolution(distance_m: float) -> float:
    """
    Distance-adaptive horizontal resolution

    Near (0-20m):   0.2° (current)
    Medium (20-50m): 0.1° (2x finer)
    Far (50m+):     0.05° (4x finer)
    """
    if distance_m < 20:
        return 0.2
    elif distance_m < 50:
        return 0.1
    else:
        return 0.05
```

**効果**:
- 40-60m の復元率: 28.1% → **60%+** (推定)
- 60-100m の復元率: 1.6% → **40%+** (推定)

**トレードオフ**:
- メモリ使用量増加 (グリッドサイズ: 1800 → 7200ステップ)
- 計算時間増加

### 改善策2: 複数点保持（k-最近傍） ⭐⭐

**概要**: 各グリッドセルで複数の点を保持

**実装案**:
```python
# 現在: 最近傍のみ保持
depth_map[key] = min_depth_point

# 改善: 上位k個を保持
depth_map[key] = [closest_points[:k]]
```

**効果**:
- 遠距離で同一セルに投影される複数点を復元可能
- 復元率: +10-15% (推定)

**トレードオフ**:
- データ構造の複雑化
- ファイルサイズ増加

### 改善策3: 垂直角度の補間 ⭐⭐⭐

**概要**: 隣接チャンネル間で線形補間

**実装案**:
```python
def interpolate_vertical_angles(v_angles: list, target_channels: int = 128):
    """
    Interpolate from 64 to 128 virtual channels
    """
    # 既存の64チャンネル間に仮想チャンネルを追加
    # 補間により、垂直方向の解像度を2倍に
```

**効果**:
- 垂直方向の角度カバレッジ向上
- 最上部・最下部の復元率改善: +15-20% (推定)

**トレードオフ**:
- チャンネル数増加: 64 → 128
- 計算量増加

### 改善策4: 確率的マッピング ⭐

**概要**: 点をグリッドセルに確率的に分配

**実装**:
- 点が2つのセル境界に近い場合、両方のセルに分配
- 重み付けで最終的な点を決定

**効果**:
- グリッド境界での点の損失を軽減
- 復元率: +5% (推定)

---

## 推奨される改善の優先順位

### フェーズ1: 即座に実装可能
1. **垂直角度の補間** (改善策3)
   - 実装難易度: 低
   - 効果: 高（特に仰角の問題を解決）
   - メモリ: 2倍増加（許容範囲）

### フェーズ2: 中期的改善
2. **適応的水平解像度** (改善策1)
   - 実装難易度: 中
   - 効果: 非常に高（遠距離問題を解決）
   - メモリ: 4-8倍増加（検討が必要）

### フェーズ3: 長期的改善
3. **複数点保持** (改善策2)
   - 実装難易度: 中
   - 効果: 中
   - データ構造の変更が必要

---

## 結論

### 現状の制約
- 固定グリッド (64 × 1800) では、遠距離・極端な仰角で物理的限界がある
- 特に **40m以上**、**±25度以上の仰角** で顕著な復元率低下

### 最も効果的な改善
1. **垂直チャンネルの増加** (64 → 128) による仰角問題の解決
2. **適応的水平解像度** による遠距離問題の解決

これらを組み合わせることで、総合復元率を **73% → 85%+** に向上させることが期待できます。

### トレードオフ
- メモリ使用量: 現在の 5-10倍
- 計算時間: 2-3倍
- しかし、より高品質なLiDARシミュレーションが可能に

---

## 次のステップ

1. ✅ 問題の分析完了
2. ⏭️ 垂直角度補間の実装（プロトタイプ）
3. ⏭️ 適応的水平解像度の実装（プロトタイプ）
4. ⏭️ 改善版での復元率測定
5. ⏭️ パフォーマンスとメモリのベンチマーク
